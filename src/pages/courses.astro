---
import Modal from '../components/Modal.astro';
import Voice from '../components/Voice.astro';
import { COURSES } from '../constants';
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';

const title = "コース説明"
---

<Layout title={title}>
  <section class="courses">
    <div class="courses__outer">
      <h2>コースについて</h2>
    </div>

    <div class="courses__wrapper">
      <div class="courses__container">
        {
          COURSES.map(({title, content, image, alt, modalId, modalTitle, modalLearnings, modalContent}) => (
            <div class="courses__card">
              <div class="courses__card-box">
                <h3>{title}</h3>
                <p>{content}</p>
                <button class="button" data-micromodal-trigger={modalId}>
                  <span>&rang;&rang;</span>CHECK THIS<span>&lang;&lang;</span>
                </button>
                <Modal id={modalId} title={modalTitle} learnings={modalLearnings} content={modalContent} />
              </div>
              <div class="courses__card-img">
                <img src={image} alt={alt} />
              </div>
            </div>
          ))
        }
      </div>
      <div class="courses__text-box">
        <h3>終了までの目安</h3>
        <p>
          PlayGroundではコースを問わず、コースを修了するまでは週10時間以上の勉強が求められます。学習時間はポモドーロテクニックを用いて計測するので、効率を高めながら集中して学習に取り組めるようになります。平均すると1日、1~2時間の勉強時間ですが、コースを進めるうちに要領を掴み、週10時間以上勉強することは徐々に容易になっていきます。コースを修了した学生の中には週20時間以上の勉強をコンスタントに続ける人も少なくありません。
        </p>
      </div>
    </div>
  </section>
  <Voice />
</Layout>

<style>
  h3 {
    margin-bottom: 30px;
  }

  ul {
    margin-top: 20px;
    margin-bottom: 20px;
  }

  li {
    line-height: 2;
    font-size: 1rem;
  }


  a {
    margin: 35px 0 0 auto;
  }

  img {
    width: 100%;
  }

  .button span {
    margin: 0 15px;
  }

  .courses__outer {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 325px;
    background-image: url(/will-promotion.github.io/images/bgi_courses_sky.jpg);
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
  }

  .courses__wrapper {
    padding: 0 4%;
  }

  .courses__container {
    max-width: 1140px;
    margin: 100px auto 0;
  }

  .courses__card {
    margin: 90px auto 0;
    padding: 40px;
    box-shadow: 0px 5px 12px #cad4e2, -6px -6px 12px #ffffff;
    border-radius: 15px;
    display: flex;
    gap: 0 40px;
    justify-content: center;
    align-items: center;
  }

  .courses__card:nth-of-type(2n) {
    flex-direction: row-reverse;
  }

  .courses__card:nth-of-type(1) {
    border: 2px solid #f7c8da;
  }

  .courses__card:nth-of-type(2) {
    border: 2px solid #d7f484;
  }

  .courses__card:nth-of-type(3) {
    border: 2px solid #b5c97c;
  }

  .courses__card:nth-of-type(4) {
    border: 2px solid #f5b8b5;
  }

  .courses__card:nth-of-type(5) {
    border: 2px solid #fde7a8;
  }

  .courses__card:nth-of-type(6) {
    border: 2px solid #ffc0d1;
  }

  .courses__card-box {
    flex-basis: 70%;
  }

  .courses__card-img {
    flex-basis: 300px;
  }

  .courses__text-box {
    max-width: 1140px;
    margin: 110px auto 100px;
  }

  @media screen and (max-width: 767px) {
    h3 {
      font-size: 1.5rem;
      text-align: center;
    }

    p {
      font-size: 1rem;
      line-height: 1.6;
    }

    .button {
      margin: 20px auto;
    }

    .courses__card, .courses__card:nth-of-type(2n) {
      flex-direction: column;
    }

    .courses__card-img {
      flex-basis: auto;
    }
  }
</style>
